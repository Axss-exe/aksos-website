'use client'
import { useState } from 'react'
const steps=[['ENVIRONMENT','A real ecosystem gives an abstract hypothesis something to test.'],['EVENT','An event becomes meaningful through its surrounding conditions.'],['RELATIONSHIPS','ATIS makes connections inspectable; RITA tests their relevance.'],['CONSEQUENCE','The result is a question to investigate, not a fabricated claim.'],['OPPORTUNITY','CONCEPTUAL DEMONSTRATION — verify evidence before acting.']]
export function ZimbabweCaseStudy(){const [step,setStep]=useState(0);return <div className="case-study"><div className="case-map" aria-hidden="true"><span className="case-contour contour-a"/><span className="case-contour contour-b"/><span className="case-marker marker-a"/><span className="case-marker marker-b"/></div><div className="case-copy"><span className="annotation">ZIMBABWE // {steps[step][0]}</span><p>{steps[step][1]}</p><button onClick={()=>setStep((step+1)%steps.length)}>Advance story →</button></div></div>}
