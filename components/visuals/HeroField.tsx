'use client'
import { useEffect, useRef } from 'react'
const fragments = Array.from({ length: 18 }, (_, i) => ({ x: (i * 37) % 100, y: 12 + ((i * 53) % 76), length: 8 + (i % 4) * 5, tilt: (i % 3) - 1 }))
export function HeroField() { const ref = useRef<HTMLCanvasElement>(null); useEffect(() => { const canvas=ref.current; const host=canvas?.parentElement; if(!canvas||!host)return; const ctx=canvas.getContext('2d'); if(!ctx)return; let raf=0, visible=true, w=1,h=1,dpr=1; const resize=()=>{const r=host.getBoundingClientRect();w=Math.max(1,Math.floor(r.width));h=Math.max(1,Math.floor(r.height));dpr=Math.min(2,window.devicePixelRatio||1);if(canvas.width!==w*dpr||canvas.height!==h*dpr){canvas.width=w*dpr;canvas.height=h*dpr;canvas.style.width=`${w}px`;canvas.style.height=`${h}px`}};const draw=(time:number)=>{if(!visible)return;resize();ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,w,h);const reduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const drift=reduced?0:Math.sin(time*.0003)*3;fragments.forEach((f,i)=>{const x=w*f.x/100+drift*(i%2?1:-1),y=h*f.y/100;ctx.strokeStyle=`rgba(26,26,26,${.12+(i%4)*.03})`;ctx.lineWidth=.7;ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(x+f.length,y+f.tilt*9);ctx.stroke()});raf=requestAnimationFrame(draw)};const io=new IntersectionObserver(([e])=>{visible=e.isIntersecting;if(visible)raf=requestAnimationFrame(draw);else cancelAnimationFrame(raf)});const ro=new ResizeObserver(()=>{if(visible){cancelAnimationFrame(raf);raf=requestAnimationFrame(draw)}});io.observe(host);ro.observe(host);raf=requestAnimationFrame(draw);return()=>{cancelAnimationFrame(raf);io.disconnect();ro.disconnect()}},[]);return <div className="visual hero-field"><canvas ref={ref} aria-hidden="true"/><span className="visual-note">FIELD / LATENT STRUCTURE</span></div> }
export default HeroField
